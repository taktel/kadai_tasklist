<h1><%= @user.name %> のタスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.id, task %> : <%= task.content %> &gt; <%= task.status %></a></li>
  <% end %>
</ul>

<%= link_to '新規タスクの作成', new_task_path %>
